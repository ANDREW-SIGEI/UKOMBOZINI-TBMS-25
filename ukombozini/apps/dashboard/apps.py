from django.apps import AppConfig

class DashboardConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'ukombozini.apps.dashboard'
    verbose_name = 'Officer Dashboard'
